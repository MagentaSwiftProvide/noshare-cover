[repository]
name = "noshare-cover"
authors = ["gitscout-bot"]

[noshare-cover]
description = "Image or video instead of the no_screen_share black box"
authors = ["gitscout-bot"]
output = "libnoshare-cover.so"
# requires cargo (rustup or the rust package) in the user's PATH
build = ["make -j$(nproc)"]
